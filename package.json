{
  "name": "polymarket-flip-bot",
  "version": "5.0.0",
  "description": "BTC 5m flip bot (paper): wait 7s, fire any side @ 0.70, SL @ 0.50, re-enter any side @ 0.65 with 2x shares, hold to resolution",
  "main": "index.js",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "start": "node index.js",
    "smoke": "node --check engine.js && node --check index.js && node smoke.js"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
