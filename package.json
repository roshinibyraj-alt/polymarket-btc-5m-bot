{
  "name": "polymarket-limit-hedge-bot",
  "version": "3.0.0",
  "description": "Paper BTC 5m limit-hedge bot: limit-buy both sides @0.40, limit TP @0.60, market SL @0.25, one side holds to resolution",
  "main": "index.js",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "start": "node index.js",
    "smoke": "node --check engine.js && node --check index.js && node smoke.js"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
