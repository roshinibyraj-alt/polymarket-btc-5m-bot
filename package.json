{
  "name": "polymarket-flip-bot",
  "version": "4.0.0",
  "description": "BTC 5m intra-window flip bot (paper): fire whichever side ticks 0.55, alternate UP<->DOWN, 2x martingale each flip, base = 1% of capital, hold to resolution",
  "main": "index.js",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "start": "node index.js",
    "smoke": "node --check engine.js && node --check index.js && node smoke.js"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
