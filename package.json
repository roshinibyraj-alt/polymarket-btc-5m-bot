{
  "name": "polymarket-btc-momentum-bot",
  "version": "3.0.0",
  "description": "Paper BTC 5m momentum bot: follow momentum into close (~120s left, ask >= 0.70), stop-loss + early exit, CLOB order books",
  "main": "index.js",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "start": "node index.js",
    "smoke": "node --check engine.js && node --check index.js"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
