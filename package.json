{
  "name": "polymarket-flip-bot",
  "version": "5.0.0",
  "description": "BTC 5m flip bot (paper): wait 45s, first entry ONLY in 0.65\u20130.70 band (no cheap low-side buys), SL 0.50, re-enter any side \u22650.65 with 2x shares, carry on loss, hold to resolution",
  "main": "index.js",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "start": "node index.js",
    "smoke": "node --check engine.js && node --check index.js && node smoke.js"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
