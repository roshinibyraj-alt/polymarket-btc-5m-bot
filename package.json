{
  "name": "polymarket-flip-bot",
  "version": "5.0.0",
  "description": "BTC 5m CheapHunter (paper): wait 30s, buy underdog <= 0.20, TP @ 0.50, one trade per window, no SL, no martingale",
  "main": "index.js",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "start": "node index.js",
    "smoke": "node --check engine.js && node --check index.js && node smoke.js"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
